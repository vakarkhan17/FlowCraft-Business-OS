import { FoundationList } from '@/components/foundation-list';
export default function Page() { return <FoundationList title="Organization Access" subtitle="Effective-dated user access to organization nodes and descendants" endpoint="/organization/organization-access" objectCode="USER_ORGANIZATION_ACCESS" archiveEnabled={false} columns={[{key:'accessLevel',label:'Access level'},{key:'includeDescendants',label:'Include descendants'},{key:'validFrom',label:'Valid from'}]} fields={[{key:'userId',label:'User UUID',required:true},{key:'organizationNodeId',label:'Organization node UUID',required:true},{key:'accessLevel',label:'Access level',required:true},{key:'validFrom',label:'Valid from',type:'date'}]} />; }
